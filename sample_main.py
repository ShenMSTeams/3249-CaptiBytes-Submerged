try:
    import SuperSecretLib5000 as ssl5k
    from hub import port
    import runloop
 
    print("Imported (ssl5, hub.port, runloop) successfully!")
except Exception:
    print("Failed to import modules...")
